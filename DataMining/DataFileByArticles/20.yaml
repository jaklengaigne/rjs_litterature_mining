# if there is no data, leave a blank space in the according field
article:
  WOS: 000297786500056
  type: # paper type
    numerical
    experimental
  application_field: # application target, 1 or 2 max
    1D modelisation

materials:
  state: 
    solution
  name: # fullName (ABBREVIATION)
    polylactic acid (PLA)
  additive: # additive and solvents use
    chloroform

machine_characteristics:
  speed: # rpm
    min: 2000
    max: 30000
  collector_diameter: # m
    min: 27.0e-2
    max: 27.0e-2
  temperature: # K
    min:
    max:
  nozzle_diameter: # m
    min: 340.0e-6
    max: 820.0e-6
  spinneret_diameter: # m  ***At nozzle/syringe/needle tip***
    min: 2.70e-2
    max: 2.70e-2
  special:

fiber_results:
  fiber_diameter: # m
    min: 300.0e-9
    max: 6.0e-6
    average:  #format [average, error]
  fiber_morphology:
  fiber_orientation: # 0-1, 1 → perfectly aligned
  defect:
    beads
  production_rate: # kg/min
    min:
    max:
  tool:
    SEM
    viscometer
  other_tests:
    viscosity
    scaling
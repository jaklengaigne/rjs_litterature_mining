# if there is no data, leave a blank space in the according field
article:
  WOS: 000525104200001
  type: # paper type
    analytical
    experimental
  application_field: # application target, 1 or 2 max
    trajectory

materials:
  state:
    solution
  name: # fullName (ABBREVIATION)
    polyethyleneoxide (PEO)
  additive: # additive and solvents use
    deionized water

machine_characteristics:
  speed: # rpm
    min:
    max:
  collector_diameter: # m
    min: 40.0e-2
    max: 40.0e-2
  temperature: # K
    min:
    max:
  nozzle_diameter: # m
    min: 0.58e-3
    max: 0.58e-3
  spinneret_diameter: # m  ***At nozzle/syringe/needle tip***
    min:
    max:
  special:

fiber_results:
  fiber_diameter: # m
    min:
    max:
    average:  #format [average, error]
  fiber_morphology:
  fiber_orientation: # 0-1, 1 → perfectly aligned
  defect:
  production_rate: # kg/min
    min:
    max:
  tool:
    data acquisition box (USB-6002)
    hygrometer
    high-precision density meter
    rheometer
    tensiometer
    canny edge detector
  other_tests:
    trajectory
# if there is no data, leave a blank space in the according field
article:
  WOS: 000262343500008
  type: # paper type
    experimental
  application_field: # application target, 1 or 2 max
    yttrium aluminum garnet

materials:
  state: 
    solution
  name: # fullName (ABBREVIATION)
    yttrium aluminum garnet (YAG)
  additive: # additive and solvents use
    water

machine_characteristics:
  speed: # rpm
    min: 3000
    max: 4000
  collector_diameter: # m
    min:
    max:
  temperature: # K
    min:
    max:
  nozzle_diameter: # m
    min: 0.2e-3
    max: 0.2e-3
  spinneret_diameter: # m  ***At nozzle/syringe/needle tip or hole***
    min:
    max:
  special:
    drum-collector
    collector with controlled heating
    external air flow
    number of nozzle, 840

fiber_results:
  fiber_diameter: # m
    min: 3.0e-6
    max: 10.0e-6
    average:  #format [average, error]
      [nominal, +-]
  fiber_morphology:
  fiber_orientation: # 0-1, 1 → perfectly aligned
  defect:
  production_rate: # kg/min
    min:
    max:
  tool:
     TG/DTA
     XRD
     FT-IR
     SEM
     electronic single fiber strength tester (AGS-50NG)
  other_tests:
    grain size
    tensile strength
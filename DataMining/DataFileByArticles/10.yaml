# if there is no data, leave a blank space in the according field
article:
  WOS: 000412813000005
  type: # paper type
    experimental
  application_field: # application target, 1 or 2 max
    mass production
    collector

materials:
  state: 
    solution
  name: # fullName (ABBREVIATION)
    polyvinylpyrrolidone (PVP)
  additive: # additive and solvents use
    distilled water

machine_characteristics:
  speed: # rpm
    min: 2000
    max: 5500
  collector_diameter: # m
    min:
    max:
  temperature: # K
    min: 298.15
    max: 298.15
  nozzle_diameter: # m
    min: 0.159e-3
    max: 0.337e-3
  spinneret_diameter: # m
    min:
    max:
  special:
    electrostatic-assisted
    vertical electrostatic force
    convey-collector
    air-sealed
    relative humidity, 50 %
    distance between nozzle and convey-collector, 12.0e-2 m
    electric field intensity, 8000 V

fiber_results:
  fiber_diameter: # m
    min: 500.0e-9
    max: 1848.0e-9
    average:  #format [average, error]
  fiber_morphology:
  fiber_orientation: # 0-1, 1 → perfectly aligned
  defect:
  production_rate: # kg/min
    min:
    max:
  tool:
    FESEM
  other_tests:
    jet drawing